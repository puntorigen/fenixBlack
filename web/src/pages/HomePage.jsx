import React, { useRef, useState } from 'react';
import { WiredCard, WiredButton, WiredInput } from 'react-wired-elements';

function HomePage() {
    return (
        <h1 style={{ color:'white' }}>Choose an Example :)</h1>
    );
} 

export default HomePage;